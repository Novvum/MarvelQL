{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "clean": "rimraf packages/**/lib",
    "start": "lerna run --parallel start --scope @marvelql/graphql"
  },
  "devDependencies": {
    "@types/node": "^8.0.28",
    "lerna": "3.4.3",
    "rimraf": "^2.6.2",
    "typescript": "2.9.2"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/graphql",
      "**/graphql/**"
    ]
  }
}